import React from "react";
import Excel from "../../Containers/Excel/Excel";

/**
 * ExcelPage Component serves as a page for displaying the Excel container. It simply renders the Excel container, which likely contains the main functionality for handling Excel-related tasks.
 * @returns {JSX.Element} The JSX representation of the ExcelPage component.
 */
const ExcelPage = () => {
  return (
    <div>
      <Excel />
    </div>
  );
};

export default ExcelPage;
